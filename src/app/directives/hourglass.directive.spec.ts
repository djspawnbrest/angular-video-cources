import { HourglassDirective } from './hourglass.directive';

describe('HourglassDirective', () => {
  it('should create an instance', () => {
    const directive = new HourglassDirective();
    expect(directive).toBeTruthy();
  });
});
